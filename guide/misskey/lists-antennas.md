# 列表与天线

列表和天线是两种定制信息流的工具：列表按**人**组织，天线按**关键词**组织。

## 列表

列表（Lists）是你自建的用户分组。把感兴趣的用户加入同一个列表，即可只看他们（以及可选的「包含回复」）的帖子。

- 通过侧栏「列表」创建，例如「好友」「画师」「新闻源」。
- 列表时间线可以在[时间线](/guide/posts#时间线)中切换查看。
- 列表是私有的，只有你自己能看到列表内容和成员。
- 列表上限和每个列表的成员数上限由服务器策略决定。

## 天线

天线（Antennas）按条件自动聚合帖子，比如「追踪包含某个关键词的所有新帖」。

- 通过侧栏「天线」创建，每个天线有独立时间线。
- 可设置的条件包括：
  - **关键词**：匹配帖文内容，可指定全部匹配或任一匹配
  - **来源**：关注用户 / 本站所有用户 / 指定用户列表
  - **帖子类型**：如仅匹配带附件的帖子
- 适合持续追踪特定话题、角色名或活动标签。

## 列表 vs 天线怎么选

- 想持续看**某些人**的动态 → 用列表。
- 想追踪**某类内容**（无论谁发的）→ 用天线。
