# 对话风格

对话风格决定角色「**怎么说**」：语气、人称、句长、是否带动作描写和旁白。同一个角色配不同风格，聊天体验完全不同——高冷人设也可以有话痨风格。

对话风格和角色一样需要**单独创建、单独审核**，并且是社区游玩的硬性前提：发送消息前必须为会话选择一个风格。

## 获取风格

两种途径：

- **自己创建**（见下文）。
- **从广场加入**：在广场的「对话风格」标签页浏览别人发布的风格，点「加入我的列表」即可订阅使用。来自广场的风格会带标识，可随时移除。

## 创建风格

进入「智能体 → 创作 → 对话风格」新建：

- **风格名称**：列表里显示的名字。
- **简介**：只用于展示，**不会写入模型提示词**。
- **风格说明（正文）**：真正起作用的部分，写入系统提示。描述语气、人称、回复长短、是否使用动作描写/旁白、标点习惯等。

写正文同样适用「具体优于笼统」：

- 笼统：「语气自然。」——模型不知道什么是你的「自然」。
- 具体：「第一人称口语，句子偏短，每两三句夹一个动作描写（*斜体*），几乎不用感叹号，吐槽时习惯先停顿一拍。」

风格会与会话中的角色设定**合并**生效：角色管「是什么人」，风格管「怎么说话」。

## 编辑与发布

风格和角色一样支持：草稿测试、版本管理、提交审核、发布到广场。流程见[创作角色](/guide/agent/create#发布与审核)。

## 在会话中切换

会话顶部的「对话风格」标签页可以随时切换当前会话的风格，不影响角色设定。测试同一个角色配不同风格的差别就靠它。

::: tip
第一次游玩提示「没有可用的对话风格」时，去风格广场加一个，或者干脆自己写一份——通常十分钟内就能写完。
:::
