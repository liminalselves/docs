# 用量与额度

智能体的对话、生图、识图、便签压缩都消耗**额度**。在「我的用量」页面管理一切和额度有关的事。

## 额度从哪来

- **每日签到**：每天签到可以领取额度。奖励有随机成分（「今日手气」），连续签到有加成，特定身份组有额外倍率，节日可能有活动加成。
- **卡密兑换**：管理员发放的充值卡密，在「我的用量」页输入即可兑换。
- **管理员签发**：活动奖励等由管理员直接发放。

## 费用怎么算

- **对话**：按模型每张卡片标注的单次费用计费。不同模型价格不同，切换模型即切换单价。
- **生图**：按所选生图模型的「每张费用」计费。
- **识图**：发送图片触发识图时按次计费。
- **便签压缩**：为会话单独选择压缩模型时，摘要按该模型单独计费，与主回复分开扣（时机可能略有先后）。

发送前，「模型」标签页会显示**预计单次发送费用**，卡片上标注「免费」的模型不扣额度。

## 用量记录

「我的用量」列出每次调用记录：模型、费用、状态，方便对账。失败的请求**不计费**。

## 余额不足

提示「智能体对话余额不足，无法发送」时，去签到或兑换卡密。生图遇到「额度不足」同理。

::: tip
想在额度内玩得更久：把长对话交给便签压缩（减少重复发送原文的开销），或切换到费用更低的模型聊日常，重要剧情再切高质量模型。
:::
